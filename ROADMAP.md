# نقشه‌ی راه بهبود قابلیت‌به‌قابلیت «تجارت‌یار»

> وضعیت: «اقیانوس وسیع ولی کم‌عمق» — چندین کار انجام می‌شود اما هیچ‌کدام کامل نیست.
> روش: هر قابلیت را با ۳ بخش بررسی می‌کنیم: **وظیفه → عمق فعلی → برنامه‌ی بهبود**،
> سپس یکی‌یکی تا «عمق قابل‌قبول» جلو می‌بریم.

## جدول کلی (عمق = ۱ تا ۵)

| # | قابلیت | عمق فعلی | اولویت |
|---|---|---|---|
| ۱ | مدیریت کارگو و موجودی | ۳ → ۴ ✅ | ⭐ هسته |
| ۲ | گردش کار پرونده‌ها (کانبان) | ۲.۵ → ۳ ✅ | ۲ |
| ۳ | ویزارد ارزیابی واردات | ۲.۵ → ۴ ✅ | ⭐ ۱ |
| ۴ | استعلام قیمت (RFQ) | ۲ | ۳ |
| ۵ | تفکیک تعرفه HS | ۳ → ۴ ✅ | ⭐ ۲ |
| ۶ | اعتبارسنجی تأمین‌کننده | ۲.۵ → ۳.۵ ✅ | ۲ |
| ۷ | کاوشگر هوش تجاری | ۲ | ۳ |
| ۸ | داشبورد تحلیلی + دفتر مالی | ۲.۵ → ۴ ✅ | ⭐ ۱ |
| ۹ | شناسنامه منابع داده | ۳ (برچسب‌گذاری شد) | ✅ انجام |
| — | لایه AI + موتور بهای تمام‌شده | ۳ → ۴ ✅ | ⭐ ۲ |

---

## ۱) مدیریت کارگو و موجودی — عمق ۳/۵ ✅ انجام‌شده

**وظیفه:** کارتابل پرونده‌ها، جستجو، فیلتر دسته، خروجی CSV، ثبت و مشاهده‌ی پرونده.

**اقدامات انجام‌شده:**
- ✅ قرارداد واحد پول مستند شد (`landedCostToman`/`marketPriceToman` = میلیون تومان) + هلپر `fmtTomanSmart`.
- ✅ حذف ضریب سلیقه‌ای `Math.min(stockQty,10)` در محاسبه‌ی ارزش سبد.
- ✅ اتصال `patchUnit` → فرم «ویرایش پرونده» در `UnitDetailModal` (تأیید End-to-End با PATCH).
- ✅ CSV امن (RFC 4180 + BOM) و جداسازی منطق به `lib/csv.ts`.
- ✅ محدوده‌ی صادقانه‌ی جستجوی هدر (فقط نمای کارتابل) + placeholder صحیح.
- ✅ منبع واحد وضعیت‌ها و دسته‌ها (`ALL_STATUSES`، `isSettledStatus`، `PRODUCT_CATEGORIES`).
- ✅ اصلاح برچسب واحد «B» → «میلیارد ت» در hero.

**مشکلات کلیدی (تأییدشده):**
- ❌ **ناسازگاری واحد پول:** `landedCostToman` در type به‌عنوان «میلیون تومان» تعریف شده، ولی جاهای مختلف با «تومان خام» و «میلیارد» قاطی می‌کنند → همان عدد در صفحات مختلف فرق می‌کند.
- ❌ **فرمول سلیقه‌ای** در `MetricsOverview`: برای واحدهای تنی/کیسه‌ای `Math.min(stockQty, 10)` اعمال می‌شود (بدون منطق).
- ❌ **ویرایش پرونده مرده است:** `patchUnit` و API آن کامل هست ولی هیچ UI ای ندارد (فقط تغییر وضعیت ممکن است).
- ❌ **جستجوی سراسری دروغ است:** باکس جستجوی هدر می‌گوید «جستجو در کارگو، تعرفه، تأمین‌کننده…» ولی فقط جدول موجودی را فیلتر می‌کند.
- ❌ CSV با string concatenation ساخته می‌شود (کاما/کوتشن داخل نام‌ها خراب می‌شود).
- ❌ لیست وضعیت‌ها در ۳ فایل تکراری hardcode شده.

**برنامه‌ی بهبود:**
۱. یکسان‌سازی واحد پول (یک قرارداد رسمی + تابع تبدیل مرکزی).
۲. حذف `Math.min` سلیقه‌ای و محاسبه‌ی صحیح ارزش.
۳. اتصال `patchUnit` → فرم «ویرایش پرونده» در `UnitDetailModal`.
۴. CSV امن (escape صحیح) + جداسازی منطق به `lib/csv.ts`.
۵. محدوده‌ی واقعی جستجوی هدر یا تغییر placeholder.
۶. یک منبع واحد برای وضعیت‌ها (`STATUS_FLOW` + حالت رزرو).

---

## ۲) گردش کار پرونده‌ها (کانبان) — عمق ۳/۵ ✅ انجام‌شده

**وظیفه:** جابه‌جایی پرونده بین مراحل، تایم‌لاین، هشدار معطلی، حذف.

**اقدامات انجام‌شده:**
- ✅ فرم «ثبت یادداشت» روی هر کارت → اتصال `POST /api/inventory/:id/events` از طریق متد جدید `addEvent` در store و `api.addEvent`؛ یادداشت بلافاصله در تایم‌لاین (۵ رویداد آخر) ظاهر می‌شود.
- ✅ منوی تغییر وضعیت مستقیم به هر یک از `ALL_STATUSES` (شامل وضعیت رزرو/پیش‌فروش) جایگزین پرش صرفاً ±۱ مرحله شد؛ مدیریت وضعیت رزرو در ستون انبار حفظ شد و فلش‌های ±۱ همچنان برای جابه‌جایی سریع باقی ماندند.
- ⏸️ Drag & Drop واقعی: خارج از این دور (پرش مستقیم + فلش پوشش کافی می‌دهد).

**راستی‌آزمایی:** `tsc --noEmit` خروجی ۰، `npm run build` خروجی ۰، `POST /api/inventory/:id/events` روی واحد واقعی تست شد (رویداد `note` با `by: کارشناس بازرگانی` ذخیره و سپس داده‌ی تست پاک شد).

---

## ۳) ویزارد ارزیابی واردات — عمق ۴/۵ ✅ انجام‌شده

**وظیفه:** ارزیابی امکان‌سنجی، محاسبه بهای تمام‌شده، ثبت در کارتابل و آرشیو.

**اقدامات انجام‌شده:**
- ✅ ادعای دروغین «۷ مرحله‌ای» در ۴ جا (App.tsx ×۲، Header، Sidebar، metadata.json) به «۴ مرحله‌ای» صادقانه اصلاح شد؛ توضیح مراحل هم با واقعیت هماهنگ شد.
- ✅ نرخ ارز از `settings.fx.usdNimaToman` خوانده می‌شود (hardcode `۶۸۰۰۰` فقط به‌عنوان fallback نهایی).
- ✅ `initialScenario` هنگام باز شدن از حل‌کننده‌ی HS واقعاً اعمال می‌شود (`useEffect` چون مودال همیشه mounted است).
- ✅ خروجی زنده‌ی `LandedCostCalculator` با `onResult(result, margin, qty)` به ویزارد وصل شد و «حکم نهایی + واحد ساخته‌شده» از همین اعداد مشتق می‌شوند (`calcResult`/`calcMargin` به‌عنوان منبع حقیقت؛ `computeLandedCost` صرفاً fallback اولیه).
- ✅ مرحله‌ی ۳ (اعتبارسنجی خارجی) از نمایش فیک («HRB-782194»، «ISO/CE/TÜV»، «امتیاز ۹۸») به فرم ممیزی واقعی + گزارش ریسک زنده‌ی `api.aiSupplierCheck` تبدیل شد؛ امتیاز اعتبار واحد ساخته‌شده از `riskScore` واقعی مشتق می‌شود (بدون ممیزی = ۵۰ خنثی).
- ✅ نام تأمین‌کننده‌ی ثبت‌شده از ورودی کاربر می‌آید، نه «Certified Manufacturer» ساختگی.

**راستی‌آزمایی:** `tsc --noEmit` خروجی ۰، `npm run build` خروجی ۰، endpoint محلی `/api/ai/supplier-check` با موتور قاعده‌محور تست شد (ریسک متوسط/امتیاز ۵۴).

**باقی‌مانده (خارج از این دور):** افزودن ۳ مرحله‌ی واقعی اختیاری (مجوزها/ارز/جمع‌بندی) — تصمیم آگاهانه: فعلاً صادقانه «۴ مرحله» است.

---

## ۴) استعلام قیمت (RFQ) — عمق ۲/۵

**وظیفه:** تولید متن رسمی RFQ + کپی/دانلود.

**مشکلات:**
- فقط انگلیسی و یک قالب ثابت؛ فارسی ندارد.
- از داده‌ی واقعی پرونده/ارزیابی پر نمی‌شود (همیشه از پیش‌فرض پنل شروع می‌شود).
- دکمه‌های «ارسال استعلام بر اساس این سند» از هوش تجاری، فقط تأمین‌کننده را عوض می‌کنند نه مشخصات کالا را.

**برنامه‌ی بهبود:**
۱. پر شدن خودکار مشخصات کالا از سند هوش تجاری/پرونده.
۲. قالب فارسی (در کنار انگلیسی) + انتخاب زبان.
۳. (اختیاری) پیش‌نمایش چاپی.

---

## ۵) تفکیک تعرفه HS — عمق ۴/۵ ✅ انجام‌شده

**وظیفه:** سناریوهای اختلافی + دایرکتوری + پیشنهاد AI/محلی.

**اقدامات انجام‌شده:**
- ✅ **حذف کد ساختگی `8479.89.00`:** در هیچ حالتی کد تعرفه‌ی حدسی تولید نمی‌شود؛ بدون تطبیق معتبر، پاسخ صادقانه‌ی «یافت نشد — توضیحات دقیق‌تر بدهید» برمی‌گردد.
- ✅ **منبع واحد نرخ تعرفه (`src/lib/hsResolution.ts`):** نرخ‌های سناریوهای اختلافی دیگر از فایل خودشان خوانده نمی‌شوند؛ در زمان نمایش با `enrichScenarioCode` از `HS_CODE_DIRECTORY` تغذیه می‌شوند (و ویزارد هم هنگام باز شدن از سناریو همین مسیر را می‌رود). drift صفر راستی‌آزمایی شد.
- ✅ **ماندگاری نتایج استعلام:** نتایج AI در `localStorage` ذخیره می‌شود (با refresh گم نمی‌شود) + دکمه‌ی «پاک کردن نتایج استعلام».
- ✅ **برچسب صادقانه:** «پوشش محلی: ۲۷ ردیف منتخب + استعلام Gemini برای سایر کالاها» به‌جای ادعای «کل ۹۸ فصل»؛ پیام موفقیت هم بین موتور Gemini و موتور محلی تفکیک می‌شود.

## ۹) لایه AI + موتور بهای تمام‌شده — عمق ۴/۵ ✅ انجام‌شده

**وظیفه:** پیشنهاد HS + تحلیل ریسک تأمین‌کننده (Gemini با fallback محلی).

**اقدامات انجام‌شده:**
- ✅ **نام مدل اصلاح شد:** `gemini-3.7-flash` (نامعتبر، fallback بی‌صدا) → `gemini-2.5-flash` (مدل مستند و پایدار Google؛ قابل تغییر با `GEMINI_MODEL`).
- ✅ **حذف دقت کاذب در fallback:** با قابلیت ۵ ادغام شد (دیگر کد `8479.89.00` تولید نمی‌شود).
- ✅ موتور بهای تمام‌شده (`costing.ts`) سالم و مستند باقی ماند و خروجی‌اش نرخ ارز را هم برمی‌گرداند (`fxRateToman`) برای ثبت دقیق در پرونده.

**راستی‌آزمایی:** `tsc --noEmit` و `npm run build` خروجی ۰؛ endpoint `/api/ai/hs-suggest` تست شد (تطبیق → کد صحیح؛ بدون تطبیق → ۰ نتیجه و بدون کد ساختگی)؛ `modelName()` → `gemini-2.5-flash`.

---

## ۶) اعتبارسنجی تأمین‌کننده — عمق ۳.۵/۵ ✅ انجام‌شده

**وظیفه:** ماتریس فیلتر، کارت ممیزی، تحلیل ریسک AI/محلی.

**اقدامات انجام‌شده:**
- ✅ فرم «افزودن تأمین‌کننده» (`SupplierFormModal`) → اتصال `POST /api/suppliers` (`db.upsertSupplier`) از طریق متد جدید `upsertSupplier` در store؛ تأمین‌کننده‌ی جدید با وضعیت «در حال ممیزی کارخانه» و `isVerified:false` ثبت می‌شود تا ادعای تأیید نادرست نشود.
- ✅ ویرایش از روی کارت (دکمه‌ی «ویرایش») → `PATCH /api/suppliers/:id` با merge موجود + بدنه؛ حذف هم با `DELETE /api/suppliers/:id` و تأییدیه‌ی مودال اضافه شد.
- ✅ پراپ dead `onOpenAssessmentForProduct` و استیت بی‌استفاده‌ی `selectedSupplierForModal` حذف شدند.
- ✅ هیوریستیک محلی ریسک در `server/ai.ts` از regexهای تک‌خطی به چک‌لیست ساختاری ۶-محوره ارتقا یافت: شخصیت حقوقی، واژه‌های واسطه‌ای، طول نام، دامنه (شامل تشخیص ایمیل رایگان + تطبیق TLD با کشور)، وعده‌های فروش تهاجمی، و مجوز تخصصی بر اساس دسته کالا.

**راستی‌آزمایی:** `tsc --noEmit` خروجی ۰، `npm run build` خروجی ۰، چرخه‌ی کامل CRUD تأمین‌کننده (POST/PATCH/DELETE) روی endpoint واقعی تست شد و `/api/ai/supplier-check` با موتور محلی جدید روی ۳ سناریو (بروکر+ایمیل رایگان، کارخانه‌ی معتبر با TLD منطبق، نام ژنریک+ناسازگاری TLD) خروجی معنادار و متمایز داد.

---

## ۷) کاوشگر هوش تجاری — عمق ۲/۵

**وظیفه:** جستجو در اسناد نمونه + فیلترها + کپی استناد.

**مشکلات:**
- جستجو `includes` ساده است (بدون نرمال‌سازی فارسی که خود پروژه در `lib/search` دارد!).
- «۱۰ موتور» فقط چیپ فیلتر است (برچسب‌گذاری صادقانه شد ✅).

**برنامه‌ی بهبود:**
۱. استفاده از موتور `matchScore` خود پروژه برای جستجوی کیفی.
۲. جستجوی فارسی/انگلیسی/اعداد با نرمال‌سازی.

---

## ۸) داشبورد تحلیلی + دفتر مالی — عمق ۴/۵ ✅ انجام‌شده

**وظیفه:** نمودارهای زنده + تنظیمات نرخ ارز + شبیه‌ساز اثر نرخ.

**اقدامات انجام‌شده:**
- ✅ **موتور بازمحاسبه‌ی «چه‌اگر» (`src/lib/fxRevaluation.ts`):** سهم ارزی هر پرونده (CIF + حقوق ورودی + سود بازرگانی + مالیات) با نرخ فرضی بازمحاسبه و سهم ریالی محلی ثابت نگه داشته می‌شود؛ نرخ تعرفه از دایرکتوری HS داخلی خوانده می‌شود.
- ✅ **شبیه‌ساز «چه‌اگر نرخ ارز» در داشبورد:** اسلایدر نرخ نیما + نمایش بهای تمام‌شده فعلی/فرضی، تغییر، سود ناخالص جدید و حساسیت سبد (هر +۱۰۰۰ تومان ≈ X میلیارد ت) + سه پرونده‌ی بیشترین تأثیرپذیری.
- ✅ **برچسب صادقانه:** تنظیمات نرخ ارز حالا صریحاً می‌گوید «مبنای محاسبات جدید است و بهای تاریخی ذخیره‌شده را تغییر نمی‌دهد» (قبلاً ادعای اثر بدون پشتوانه داشت).
- ✅ **ثبت نرخ ارز واقعی روی پرونده:** ویزارد `fxRateAtLandedToman` (نرخ به‌کاررفته در ماشین‌حساب) را ذخیره می‌کند تا بازمحاسبه‌ی آینده دقیق باشد؛ برای پرونده‌های قدیمی، نرخ مرجع از کانال ارزی پرونده (نیما/آزاد) تخمین زده می‌شود.
- ✅ **حساسیت نرخ ارز در دفتر مالی:** ردیف پایانی تراز، اثر هر ۱۰۰۰ تومان افزایش نرخ نیما بر بهای سبد را نشان می‌دهد.
- ✅ یکسان‌سازی واحد پول (از قابلیت ۱) + اتصال `settings.fx` به ویزارد/ماشین‌حساب (از قابلیت ۳) در همین راستا تکمیل شد.

**راستی‌آزمایی:** `tsc --noEmit` خروجی ۰، `npm run build` خروجی ۰، صحت ریاضی شبیه‌ساز با داده واقعی تست شد (پرونده پنل خورشیدی: نرخ ۶۸٬۰۰۰ → ۷۴٬۸۰۰ یعنی +۰.۴۸ م.ت به ازای هر واحد).

**باقی‌مانده (خارج از این دور):** بازمحاسبه‌ی خودکار با نرخ‌های رسمی نیما (نیاز به منبع داده رسمی/اتصال API).

---

## ۹) لایه AI + موتور بهای تمام‌شده — عمق ۳/۵ ⭐

**وظیفه:** پیشنهاد HS + تحلیل ریسک تأمین‌کننده (Gemini با fallback محلی).

**مشکلات:**
- ❌ نام مدل پیش‌فرض `gemini-3.7-flash` ممکن است نامعتبر باشد → fallback بی‌صدا.
- ❌ همان کد ساختگی `8479.89.00` در fallback.
- ✅ موتور بهای تمام‌شده (`costing.ts`) سالم و مستند است.

**برنامه‌ی بهبود:**
۱. بررسی/اصلاح نام مدل (با مستندات فعلی Google).
۲. حذف دقت کاذب در fallback (با شماره ۵ ادغام می‌شود).

---

## ترتیب پیشنهادی اجرا

1. **مدیریت کارگو و موجودی** (هسته‌ی همه‌چیز + رفع ناسازگاری واحد)
2. **ویزارد ارزیابی** (رفع ادعای ۷ مرحله + اتصال نرخ ارز)
3. **داشبورد تحلیلی** (اثر واقعی نرخ ارز)
4. **تفکیک تعرفه + AI** (حذف دقت کاذب)
5. **گردش کار + تأمین‌کننده** (اتصال قابلیت‌های مرده)
6. **هوش تجاری + RFQ** (کیفیت جستجو و قالب)

> هر مرحله با `tsc --noEmit` و `npm run build` راستی‌آزمایی می‌شود.
