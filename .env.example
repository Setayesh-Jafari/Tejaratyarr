# Gemini API Key (Server-side only)
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash

# حالت ارائه/دمو — با 1 کارتابل با داده‌ی نمونه پر می‌شود
# (۱۴ پرونده، ۱۱ تأمین‌کننده، ۴ ارزیابی). در این حالت برچسب
# «داده نمونه — حالت ارائه» در هدر برنامه نمایش داده می‌شود.
# برای اجرای واقعی این خط را خالی/حذف کنید.
SEED_DEMO=1
