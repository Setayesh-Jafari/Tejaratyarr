# 🚢 اجرای تجارت‌یار از داخل ایران — راهنمای کامل

چون سرویس‌های خارجی (Render، Google Colab، پیش‌نمایش‌های e2b و…) از داخل ایران یا باز نمی‌شوند یا ناپایدارند، برای اینکه **لینکی داشته باشید که استادتان از ایران باز کند**، سه راه دارید:

| راه | لینک | زمان آماده‌سازی | هزینه |
|---|---|---|---|
| ۱. اجرای محلی روی لپ‌تاپ | `http://localhost:3000` | ۵ دقیقه | رایگان |
| ۲. استقرار روی **لیارا** (هاست ایرانی) | `https://XXXX.liara.run` | ۱۵ دقیقه | تقریباً رایگان (پلن آزمایشی) |
| ۳. Colab + cloudflared (با VPN) | `https://XXXX.trycloudflare.com` | ۱۰ دقیقه | رایگان |

---

## راه ۱ — اجرای محلی (برای دموی همین امروز)

نیازی به اینترنت خارجی نیست؛ برنامه روی کامپیوتر خودتان اجرا می‌شود و می‌توانید از طریق **اشتراک‌گذاری صفحه** (واتساپ/تلگرام/گوگل‌میت) به استاد نشان دهید.

1. Node.js را نصب کنید: به `https://nodejs.org` بروید و نسخه **LTS** را دانلود و نصب کنید.
2. فایل `Tejaratyarr.zip` را از حالت فشرده خارج کنید.
3. در پوشه‌ی `Tejaratyarr` یک ترمینال باز کنید و اجرا کنید:
   ```bash
   node dist/server.cjs
   ```
   (خروجی build از قبل آماده است — نیازی به `npm install` نیست.)
4. در مرورگر باز کنید: `http://localhost:3000`

> اگر فایل `dist` در zip نبود (نسخه‌ی قدیمی)، اول: `npm install` سپس `npm run build` سپس `node dist/server.cjs`.

---

## راه ۲ — استقرار روی لیارا (لینک دائمی ایرانی) ✅ پیشنهادی

لیارا هاست ابری ایرانی است؛ لینکی که می‌دهد (`XXXX.liara.run`) از داخل ایران بدون VPN باز می‌شود.

### آماده‌سازی (قبلاً انجام شده)
فایل `Dockerfile` و `.dockerignore` در پروژه هست؛ فقط باید استقرار دهید.

### مراحل
1. در `https://liara.ir` ثبت‌نام کنید (با ایمیل/شماره موبایل).
2. [Liara CLI](https://docs.liara.ir/cli/install) را نصب کنید:
   ```bash
   npm install -g @liara/cli
   ```
3. وارد شوید:
   ```bash
   liara login
   ```
   (ایمیل و رمز لیارا را می‌زند؛ یا با `liara login --api-token` از پنل توکن بسازید.)
4. در پوشه‌ی پروژه اجرا کنید:
   ```bash
   liara deploy --platform=docker --port=3000
   ```
   - نام برنامه را بپرسد؛ مثلاً `tejaratyarr` بدهید.
   - لیارا خودش `Dockerfile` را پیدا می‌کند، build می‌گیرد و اجرا می‌کند.
5. پس از پایان، لینک شما:
   ```
   https://tejaratyarr.liara.run
   ```
   این لینک را به استاد بدهید — **دائمی است و از ایران باز می‌شود**.

### نکته‌ها
- برنامه روی پورت `PORT` که لیارا می‌دهد گوش می‌دهد (در `server.ts` پشتیبانی شده).
- داده‌ها روی دیسک موقت است؛ برای ذخیره‌سازی دائمی می‌توانید بعداً «دیسک» به برنامه متصل کنید (`liara deploy -d data=/app/data`).
- برای فعال‌کردن هوش مصنوعی Gemini، در پنل لیارا متغیر محیطی `GEMINI_API_KEY` را اضافه کنید (اختیاری — بدون آن موتور محلی کار می‌کند).

### جایگزین: آرون‌کلود (ArvanCloud PaaS)
اگر لیارا در دسترس نبود، همین Dockerfile روی [پلتفرم ابری آرون‌کلود](https://paas.arvancloud.ir) هم با انتخاب «Docker» استقرار می‌شود.

---

## راه ۳ — Colab + cloudflared (بدون هاست، با VPN)

اگر VPN دارید و Colab برایتان باز می‌شود:
1. فایل `run_tejaratyar_colab_upload.ipynb` را در Colab آپلود کنید (File → Upload notebook).
2. سلول‌ها را به ترتیب اجرا کنید؛ در سلول آپلود، فایل `Tejaratyarr.zip` را انتخاب کنید.
3. سلول آخر یک لینک `https://XXXX.trycloudflare.com` چاپ می‌کند که از ایران قابل باز شدن است (شبکه‌ی Cloudflare در ایران در دسترس است).
4. این لینک **موقتی** است و تا وقتی Colab روشن است کار می‌کند.
