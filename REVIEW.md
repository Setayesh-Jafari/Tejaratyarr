# گزارش بازبینی فنی سامانه «تجارت‌یار»

> تاریخ بازبینی: ۱۴۰۵/۰۶/۰۷ — شاخه‌ی `arena/01a04f8e-tejaratyarr` (کامیت `b555cae`)
> روش: بازبینی دستی کل کد + اجرای `tsc --noEmit` + اجرای سرور توسعه (`tsx server.ts`)

---

## خلاصه اجرایی

حس شما درست است. این پروژه دو مشکل جدی و جدا از هم دارد:

1. **بحرانی (P0): پروژه در وضعیت فعلی اصلاً بالا نمی‌آید.** چهار ماژول داده‌ی اصلی از مخزن گم شده‌اند؛ نه کامپایل می‌شود، نه سرور استارت می‌خورد.
2. **حجم زیادی «ویترین» (façade) و کد مرده دارد.** بخش‌های قابل‌توجهی از UI اعداد، گواهی‌ها، موتورهای جستجو و «استنادهای رسمی» را نمایش می‌دهند که پشتشان هیچ داده‌ی واقعی یا اتصال زنده‌ای نیست. این دقیقاً همان بخش‌های «الکی و بیهوده» است که حس کرده‌اید.

نکته‌ی مثبت: اسکلت معماری (Store متمرکز، کلاینت API تایپ‌شده، موتور بهای تمام‌شده، نرمال‌ساز فارسی، فالبک محلی برای AI) خوب و باکیفیت نوشته شده است. مشکل، «سقفِ» بالای این اسکلت است، نه فونداسیون آن.

---

## ۱) بحرانی — پروژه کامپایل و اجرا نمی‌شود

چهار فایل داده در Git ثبت نشده‌اند ولی از سراسر پروژه import شده‌اند:

| ماژول گمشده | مصرف‌کننده‌ها |
|---|---|
| `src/data/mockData.ts` | `server/db.ts` (seed)، `TradeAssessmentModal.tsx`، `TradeIntelligenceSearch.tsx` |
| `src/data/hscodeDirectory.ts` | `server/ai.ts`، `HsCodeResolver.tsx`، `DataProvenanceView.tsx` |
| `src/data/hscodeScenarios.ts` | `App.tsx`، `HsCodeResolver.tsx`، `TradeAssessmentModal.tsx` |
| `src/data/provenanceData.ts` | `DataProvenanceView.tsx` |

شواهد:

- `npx tsc --noEmit` → **۲۴ خطا** (همگی از همین ۴ ماژول گمشده و خطاهای آبشاری آن‌ها).
- `npm run dev` → بلافاصله با خطای زیر می‌میرد:
  ```
  Error [ERR_MODULE_NOT_FOUND]: Cannot find module '.../src/data/mockData'
  imported from '.../server/db.ts'
  ```
- `npm run build` (vite build) نیز روی همین import ها متوقف می‌شود.

این فایل‌ها فقط «جزئیات تزئینی» نیستند؛ **داده‌ی seed کارتابل، دایرکتوری تعرفه، سناریوهای اختلافی و شناسنامه‌ی منابع** هستند. بدون آن‌ها هیچ صفحه‌ای معنا ندارد.

> اگر این فایل‌ها عمداً خارج از Git نگه داشته شده‌اند، باید مستند و در CI بازتولید شوند؛ در غیر این صورت این یک commit ناقص است.

---

## ۲) کد مرده، تکراری و ناهماهنگ (آشغال فنی)

| # | مورد | محل | شرح |
|---|---|---|---|
| 1 | **سرور تکراری** | `server/index.ts` (کل فایل) | کپی تقریباً یک‌به‌یک `server.ts` است با پورت ۳۰۰۱. هیچ اسکریپتی در `package.json` آن را صدا نمی‌زند. کاملاً dead است و باعث سردرگمی پورت می‌شود. |
| 2 | **عدم تطابق پورت** | `server.ts:16` ← پورت ۳۰۰۰؛ `server/index.ts:169` ← ۳۰۰۱؛ `vite.config.ts:36` ← پراکسی به ۳۰۰۱ | سرور واقعی (تک‌پروسه، با Vite middleware) روی ۳۰۰۰ است؛ پراکسی `vite.config.ts` اصلاً استفاده نمی‌شود چون Vite در حالت middlewareMode است. |
| 3 | **دستور اشتباه در UI** | `Feedback.tsx:89` | پیام خطا می‌گوید `npm run server`، اما چنین اسکریپتی وجود ندارد (فقط `dev`، `build`، `start`، `lint`). |
| 4 | **متدهای API بدون مصرف‌کننده** | `lib/api.ts` — `upsertSupplier`، `patchAssessment`؛ `AppStore.tsx` — `patchUnit` | تعریف شده ولی هیچ‌جا در UI صدا زده نمی‌شوند. |
| 5 | **اندپوینت بدون مصرف‌کننده** | `server.ts` — `POST /api/inventory/:id/events` | فرانت‌اند فقط رویدادها را می‌خواند، هرگز اضافه نمی‌کند. |
| 6 | **باگ prop (جریان قطع‌شده)** | `App.tsx:232` ← `HsCodeResolver.tsx:43` | `App` پراپ `onSelectForAssessment` می‌فرستد اما کامپوننت فقط `onSelectFinalCode(code, desc, duty)` می‌شناسد. نتیجه: **هیچ مسیری از «سناریوی اختلافی» به ویزارد ارزیابی متصل نیست** و `handleStartAssessmentWithScenario` عملاً مرده است. |
| 7 | **state/پراک مرده** | `SupplierEvaluation.tsx:37` و `:25` | `selectedSupplierForModal` مقداردهی اولیه می‌شود ولی هرگز خوانده نمی‌شود؛ پراپ `onOpenAssessmentForProduct` هم هرگز از App پاس داده نمی‌شود. |
| 8 | **خروجی‌های بی‌استفاده** | `format.ts` — `fmtUsd`؛ `search.ts` — `normalizeFa`/`tokenize`؛ `types.ts:339` — `PaginatedEvents` | در کل کد مصرفی ندارند (برخی باید private شوند). |
| 9 | **متادیتای placeholder** | `package.json:2` | `"name": "react-example"` — اسم قالب اصلی مانده. `vite` هم در dependencies و هم devDependencies تکرار شده؛ `autoprefixer` با Tailwind v4 لازم نیست. |
| 10 | **API سطحی که مصرف نمی‌شود** | `GET /api/settings`، `GET /api/suppliers`، `GET /api/inventory` | داده‌ها از `/api/bootstrap` می‌آید؛ این اندپوینت‌ها عملاً بدون مشتری‌اند (به‌عنوان سطح API می‌توانند بمانند، ولی باید آگاهانه باشد). |

---

## ۳) بخش‌های «الکی / ویترینی» — نگرانی اصلی شما

این موارد داده یا قابلیتی را نمایش می‌دهند که **پشتشان واقعیت ندارد**:

### ۳.۱ کارت «روند واردات و شاخص سلامت بازرگانی» — کاملاً ساختگی
`PerformanceSection.tsx`
- نمودار هفتگی با مقادیر دستی: `هفته ۱…هفته ۷` با `45%`، `70%`، … و «۴۲۰ میلیون تومان» و… (سطر ۶ به بعد).
- «شاخص سلامت بازرگانی و ترخیص گمرکی: **۹۴.۸**» و «رتبه الف بازرگان» و «نرخ قبولی اسناد **۹۸٪**» و «سرعت تأمین ارز **۸۶.۴٪**» — همگی hard-code شده‌اند و به هیچ داده‌ای وصل نیستند.
- این کارت درست زیر کارتابلِ واقعیِ موجودی رندر می‌شود و شبیه KPI واقعی به نظر می‌رسد، در حالی که صرفاً دکور است.

### ۳.۲ «شناسنامه منابع داده» — ادعای اتصال زنده بدون هیچ اتصالی
`DataProvenanceView.tsx`
- «سطح مستندسازی **۱۰۰٪**» hard-code است.
- ادعا می‌کند داده‌ها از «سامانه جامع انبارها (NTSW)»، «سالنامه آمار تجارت خارجی IRICA»، «ITC Trade Map»، «ImportYeti» استخراج شده‌اند — در حالی که **هیچ اتصال، واکشی یا scraping ای در کل پروژه وجود ندارد** (همه‌چیز seed استاتیک یا Gemini است).
- تایپ `integrationType` در `types.ts` گزینه‌هایی مثل «استعلام وب‌سرویس مستقیم» و «وب اسکرپینگ زنده» دارد که در UI به‌عنوان واقعیت نشان داده می‌شوند.
- جعبه‌ی «پاسخ صریح» حتی منبعِ همان نمودار ساختگی ۳.۱ را به IRICA/ITC نسبت می‌دهد.

### ۳.۳ «کاوشگر هوش تجاری با ۱۰ موتور استنادی» — فیلتر روی آرایه استاتیک
`TradeIntelligenceSearch.tsx`
- ۱۰ «موتور» (ITC Trade Map، ImportYeti، Panjiva، Apify، Perplexity و…) فقط **چیپ فیلتر روی آرایه‌ی ثابت `INTELLIGENCE_RECORDS`** هستند؛ هیچ جستجویی به اینترنت یا API آن‌ها نمی‌زند.
- «جستجوی زنده» در واقع یک `filter` کلاینت‌ساید روی همان آرایه است.
- «وضعیت تأییدشده رسمی»، «ضریب اطمینان» و «داده‌های قطعی/نیازمند راستی‌آزمایی» همگی فیلدهای دست‌نویسِ ثابت در seed هستند، نه نتیجه‌ی راستی‌آزمایی.

### ۳.۴ کارت‌های تأمین‌کننده — گواهی‌های نمایشی
`SupplierEvaluation.tsx` + seed
- برچسب‌های «**احراز اصالت رسمی**»، «ممیزی کارخانه ۹x/100»، «پایداری مالی»، «کانال پرداخت ارزی: احراز هویت و حساب بانکی پاک» همه داده‌ی ثابت seed هستند که به‌صورت نتیجه‌ی ممیزی واقعی نمایش داده می‌شوند.
- (تحلیل ریسک AI/قاعده‌محور در `AiSupplierCheckCard` صادقانه برچسب «موتور محلی» می‌خورد و اشکالی ندارد — مشکل، کارت‌های ازپیش-تأییدشده است.)

### ۳.۵ شاخص «فشردگی تعرفه» در دفتر مالی — متریک بی‌معنا
`FinancialLedger.tsx:28,33`
- نرخ ارز به‌صورت **سخت‌کد ۶۸۰۰۰** داخل فرمول آمده (به‌جای `settings.fx`).
- از نظر بُعدی هم ناسازگار است: `landedCostToman` در `types.ts` به‌عنوان «میلیون تومان» تعریف شده، ولی اینجا تقسیم بر ۶۸۰۰۰ (نرخ هر تومانِ خام) می‌شود. نتیجه‌ی «شاخص فشردگی تعرفه» قابل اتکا نیست.

### ۳.۶ قالب RFQ — ادعاهای حقوقی/زمانی منسوخ
`RfqGenerator.tsx`
- متن قالب با «On behalf of **Iranian Commercial & Import Directorate**» شروع می‌شود (القای اینکه از طرف یک نهاد دولتی است).
- پنجره‌ی تحویل «**Q3/Q4 2024**» hard-code و منسوخ است (سال ۱۴۰۵).
- دامنه‌ی فرضی `procurement@tejaratyar-trade.ir` و برچسب «آماده ارسال به کمپانی» در حالی که فقط copy/دانلود متن انجام می‌شود و هیچ ارسالی وجود ندارد.

### ۳.۷ ناسازگاری واحد پول در کل پروژه (ریسک محاسباتی)
- `types.ts`: `landedCostToman` = «میلیون تومان».
- `lib/costing.ts`: با «تومان خام» کار می‌کند (خروجی `landedPerUnitMillionToman` جدا دارد).
- `FinancialLedger.tsx`: با ۶۸۰۰۰ (تومان خام) تقسیم می‌کند.
- `App.tsx:77` و `AnalyticsDashboard`: `landedCostToman * stockQty` را با `fmtBillion` (تقسیم بر ۱۰۰۰) «میلیارد» می‌نامند، در حالی که `MetricsOverview` برای واحدهای تنی/کیسه‌ای `Math.min(stockQty, 10)` اعمال می‌کند → **همان «ارزش سبد» در جاهای مختلف اعداد متفاوتی نشان می‌دهد.**

---

## ۴) امنیت و مقیاس

- **بدون احراز هویت/مجوز:** همه‌ی اندپوینت‌های `/api/*` باز هستند؛ هر کسی می‌تواند موجودی را POST/PATCH/DELETE کند. `CORS` هم `*` است (`server.ts:18-24`).
- **پایداری:** تمام داده در یک فایل JSON (`data/appstore.json`) با نوشتن debounce ذخیره می‌شود؛ بدون پشتیبان، بدون قفل هم‌زمانی، بدون تراکنش. برای دمو مناسب، برای چند کاربر نه.
- **کلید Gemini:** درست سمت سرور نگه داشته شده (خوب). اما نام پیش‌فرض مدل در `.env.example` (`gemini-3.7-flash`) باید با مدل‌های فعلی Google تطبیق داده شود؛ اگر نام نامعتبر باشد، فراخوانی AI بی‌صدا به موتور محلی fallback می‌کند.
- **داده‌های شخصی تأمین‌کننده‌ها** (ایمیل/تلفن) به‌صورت متن ساده در seed/JSON ذخیره می‌شوند.

---

## ۵) نکات مثبت (برای انصاف)

- معماری **Store متمرکز** (`AppStore.tsx`) به‌عنوان Single Source of Truth تمیز است؛ همه‌ی تغییرات از مسیر API ثبت می‌شود.
- **کلاینت API تایپ‌شده** (`lib/api.ts`) و جداسازی منطق در `lib/format`، `lib/costing`، `lib/search` خوب است.
- **موتور بهای تمام‌شده** (`lib/costing.ts`) فرمول CIF/حقوق/سود بازرگانی/مالیات را مستند و اصولی پیاده کرده و تحلیل حساسیت نرخ ارز دارد.
- **فالبک محلی برای AI** (`server/ai.ts`) طراحی درستی است: بدون کلید Gemini، سامانه همچنان با موتور قاعده‌محور کار می‌کند و UI صادقانه «موتور محلی» را نشان می‌دهد.
- **نرمال‌ساز فارسی** (`lib/search.ts`) قوی و قابل‌استفاده‌ی مجدد است (حروف عربی، اعراب، نیم‌فاصله، ارقام).
- UI/UX، راست‌چین‌بودن و سیستم طراحی (`tj-*` در `index.css`) منسجم است.

---

## ۶) توصیه‌های اولویت‌بندی‌شده

**P0 — پروژه را قابل اجرا کنید (بدون این، بقیه بی‌معناست)**
1. بازگرداندن/بازتولید ۴ فایل `src/data/*` (حداقل نسخه‌ی حداقلی seed تا پروژه بالا بیاید).
2. حذف `server/index.ts` و هماهنگ‌کردن پورت‌ها (فقط `server.ts` روی ۳۰۰۰) + پاک‌کردن پراکسی بلااستفاده‌ی `vite.config.ts` یا اصلاح آن.
3. اصلاح متن خطای `Feedback.tsx` به `npm run dev`.

**P1 — بخش‌های الکی را یا حذف کنید یا صادقانه برچسب بزنید**
4. `PerformanceSection` را به داده‌ی واقعی کارتابل وصل کنید یا کل کارت را بردارید.
5. `DataProvenanceView` را از «ادعای اتصال زنده» به «مستندات متدولوژی/منابع مرجع (غیرزنده)» تغییر دهید؛ برچسب‌های `integrationType` را با واقعیت هم‌خوان کنید.
6. `TradeIntelligenceSearch` و `SupplierEvaluation` را صریحاً «داده‌ی نمونه/الگو» معرفی کنید تا با گزارش واقعی اشتباه گرفته نشود.
7. باگ prop `HsCodeResolver` را اصلاح کنید تا مسیر سناریو→ارزیابی کار کند (یا مسیر را حذف کنید).
8. کد مرده (متدهای API، state، خروجی‌ها) را پاکسازی کنید.

**P2 — کیفیت و مقیاس**
9. واحد پول را در کل پروژه یکسان کنید (یک واحد رسمی: تومان خام یا میلیون تومان) و `68000` سخت‌کد را با `settings.fx` جایگزین کنید.
10. «شاخص فشردگی تعرفه» را با فرمول درست بازنویسی یا حذف کنید.
11. در صورت استفاده‌ی واقعی از API، حداقل احراز هویت سبک و CORS محدود اضافه کنید؛ تاریخ/دامنه‌های منسوخ RFQ را پویا کنید.
12. نام مدل Gemini و اسکریپت‌ها/متادیتای `package.json` را تمیز کنید.

---

## جمع‌بندی نهایی

این پروژه **اسکلت خوبی دارد ولی به‌عنوان «محصول» فعلاً دو ایراد بنیادی دارد: (الف) بالا نمی‌آید و (ب) بخش بزرگی از آن ویترین است.** اگر هدف دموی سریع است، کافی است فایل‌های داده برگردند و بخش‌های ساختگی (PerformanceSection، Provenance، برچسب‌های تأیید تأمین‌کننده) یا حذف شوند یا صادقانه «نمونه» نام بگیرند. اگر هدف محصول واقعی است، باید به‌جای آرایه‌های استاتیک، اتصال واقعی (حتی حداقلی) به منابع داده اضافه شود و امنیت/یکتاسازی واحد پول در اولویت قرار گیرد.
