# 🎬 راهنمای ارائه‌ی «تجارت‌یار» روی Google Colab

> هدف: در کمتر از ۲ دقیقه سامانه را روی Colab بالا بیاورید و ۱۲ دقیقه ارائه‌ی بی‌نقص بدهید.
> دفترچه‌ی اصلی ارائه: **`presentation_colab.ipynb`** (همه‌چیز داخل خودش هست).

---

## ۱) انتخاب مسیر اجرا

| دفترچه | کی استفاده شود | نیاز به GitHub |
|---|---|---|
| `presentation_colab.ipynb` | **پیشنهادی برای ارائه** — تست سلامت، بازنشانی داده، سناریوی ارائه | بله |
| `run_tejaratyar_colab.ipynb` | اجرای سریع و ساده از GitHub | بله |
| `run_tejaratyar_colab_upload.ipynb` | وقتی GitHub از شبکه‌ی شما بسته/کند است (آپلود `Tejaratyarr.zip`) | خیر |

هر سه دفترچه سرور را با `SEED_DEMO=1` اجرا می‌کنند (بخش ۳ را ببینید).

**در Colab:** `File → Open notebook → GitHub` (یا آپلود فایل) → `Runtime → Run all`.

> ⚠️ اگر `Runtime → Change runtime type` روی GPU باشد، CPU را انتخاب کنید؛ برنامه به GPU نیاز ندارد و CPU پایدارتر است.

---

## ۲) چک‌لیست قبل از ارائه (۱۵ دقیقه زودتر)

- [ ] سلول ۱ اجرا شده و پیام «🎉 تجارت‌یار آماده‌ی ارائه است» چاپ شده.
- [ ] **سلول ۴ (تست سلامت)** همه‌ی ۷ بررسی ✅ شده‌اند.
- [ ] سلول ۲ یا ۳ اجرا شده و برنامه دیده می‌شود (یک‌بار Refresh بزنید تا فونت وزیرمتن کامل لود شود).
- [ ] روی هر ۵ بخش اصلی یک‌بار کلیک کرده‌اید (نمودارها کشیده شده‌اند).
- [ ] اگر لینک عمومی می‌دهید: لینک را در یک مرورگر دیگر (یا گوشی) باز کرده‌اید.
- [ ] زبانه‌ی Colab و زبانه‌ی برنامه جدا باز هستند؛ Colab را **باز نگه دارید** (بستن = خاموش شدن سرور).
- [ ] شارژ لپ‌تاپ/اینترنت پشتیبان (هات‌اسپات گوشی) آماده است.

---

## ۳) درباره‌ی داده‌ی ارائه (مهم)

سرور به‌صورت پیش‌فرض **خالی و صادق** بالا می‌آید — یعنی هیچ پرونده‌ی ساختگی ندارد. برای ارائه این خوب نیست،
پس یک **حالت ارائه** اضافه شده است:

| متغیر محیطی | رفتار |
|---|---|
| (بدون متغیر) | کارتابل خالی؛ کاربر داده‌ی واقعی خودش را ثبت می‌کند |
| `SEED_DEMO=1` | ۱۴ پرونده‌ی کارگو، ۱۱ تأمین‌کننده و ۴ پرونده‌ی ارزیابی نمونه بارگذاری می‌شود |

در حالت ارائه، هدر برنامه برچسب **«داده نمونه — حالت ارائه»** نشان می‌دهد تا داده‌ی نمونه با داده‌ی واقعی اشتباه نشود.

اندپوینت‌های مخصوص این حالت (فقط وقتی `SEED_DEMO=1` باشد کار می‌کنند، وگرنه `403`):

```bash
curl http://localhost:3000/api/demo/state      # وضعیت و شمارش داده
curl -X POST http://localhost:3000/api/demo/seed   # بازنشانی به داده‌ی نمونه
curl -X POST http://localhost:3000/api/demo/clear  # خالی کردن کارتابل
```

> نکته‌ی فنی: بهای تمام‌شده‌ی پرونده‌های ارزیابیِ نمونه hard-code نیست؛ با همان موتور
> `computeLandedCost` محاسبه می‌شود که ویزارد ارزیابی استفاده می‌کند — پس عدد دمو با ماشین‌حساب سامانه سازگار است.

> اگر سروری که قبلاً با `SEED_DEMO=1` اجرا شده را بدون این متغیر اجرا کنید، داده‌ی نمونه در
> `data/appstore.json` باقی می‌ماند و سرور یک هشدار صریح در لاگ چاپ می‌کند. برای شروع کاملاً واقعی،
> فایل `data/appstore.json` را پاک کنید.

---

## ۴) سناریوی ارائه — ۱۲ دقیقه

| # | بخش | زمان | چه نشان دهید | جمله‌ی کلیدی |
|---|---|---|---|---|
| ۱ | قلاب | ۱:۰۰ | هیچ (فقط صحبت) | «واردکننده سه تصمیم پرهزینه دارد: چه کالایی، از چه کسی، با چه بهایی.» |
| ۲ | نمای کلی + کارتابل کارگو | ۲:۰۰ | شاخص‌های سبد، جستجو، فیلتر، خروجی CSV، تایم‌لاین یک پرونده | «هر عدد یا از داده‌ی ثبت‌شده می‌آید یا برچسب نمونه دارد.» |
| ۳ | گردش کار پرونده‌ها | ۱:۳۰ | کانبان، جابه‌جایی مرحله، هشدار معطلی، ثبت یادداشت | «وضعیت پرونده از یادداشت تیم جدا نیست.» |
| ۴ | **ویزارد ارزیابی واردات** | ۳:۰۰ | سناریوی آماده → بهای تمام‌شده → تفکیک CIF/حقوق ورودی/سود بازرگانی/ارزش افزوده → حاشیه سود و حساسیت ارز | «فرمول‌ها قواعد گمرک ایران است، نه عدد دستی.» |
| ۵ | تفکیک تعرفه HS | ۱:۰۰ | جستجوی کالا → کد، ماخذ، گروه صمت، سناریوی اختلافی | «عدد تعرفه از دایرکتوری محلی می‌آید تا نرخ توهمی تولید نشود.» |
| ۶ | اعتبارسنجی تأمین‌کننده | ۱:۰۰ | امتیاز ریسک، مدارک، سطح اطمینان منبع | «بدون کلید Gemini هم موتور محلی کار می‌کند و UI همین را می‌گوید.» |
| ۷ | داشبورد تحلیلی و مالی | ۱:۳۰ | نمودارها، ارزیابی مجدد ارزی، اثر تغییر نرخ | «تصمیم با سناریو گرفته می‌شود، نه با یک عدد.» |
| ۸ | جمع‌بندی | ۱:۰۰ | هیچ | «React 19 + TypeScript، Express، ذخیره‌سازی JSON اتمیک، AI به‌عنوان لایه‌ی افزایشی.» |

### متن پیشنهادی برای شروع

> «سامانه‌ی تجارت‌یار برای یک مسئله‌ی مشخص ساخته شده: واردکننده‌ی ایرانی امروز تصمیم‌های پرهزینه‌ای
> می‌گیرد بدون اینکه بهای تمام‌شده‌ی واقعی و ریسک تأمین‌کننده را یک‌جا ببیند. این سامانه همان سه تصمیم را
> در یک کارتابلِ شواهدمحور نشان می‌دهد. اجازه بدهید از خودِ برنامه شروع کنم.»

---

## ۵) پرسش‌های احتمالی و پاسخ کوتاه

| سؤال | پاسخ آماده |
|---|---|
| داده‌ها واقعی است؟ | داده‌ی کارتابل نمونه است و در UI برچسب دارد؛ قواعد محاسبه و دایرکتوری تعرفه واقعی‌اند. |
| چرا Colab؟ | فقط برای دمو؛ استقرار واقعی با `Dockerfile` روی هاست ایرانی (مثلاً لیارا) — راهنما در `DEPLOY_IRAN.md`. |
| اگر Gemini نباشد؟ | موتور قاعده‌محور محلی جایگزین می‌شود؛ وضعیت در هدر برنامه شفاف نمایش داده می‌شود. |
| کلید API کجاست؟ | فقط سمت سرور (`GEMINI_API_KEY`)؛ هیچ‌وقت به مرورگر نمی‌رسد. |
| مقیاس‌پذیری؟ | لایه‌ی ذخیره‌سازی طوری نوشته شده که ارتقا به SQLite بدون تغییر رابط توابع ممکن باشد. |
| امنیت؟ | اندپوینت‌های داده‌ی نمونه با `SEED_DEMO` محافظت می‌شوند (`403` در حالت عادی)؛ در استقرار واقعی باید احراز هویت اضافه شود. |
| چقدر طول کشید؟ | به معماری و عمق قابلیت‌ها اشاره کنید — جدول عمق در `ROADMAP.md`. |

---

## ۶) 🆘 نقشه‌ی نجات (وقتی وسط ارائه چیزی خراب می‌شود)

| علامت | تشخیص | اقدام |
|---|---|---|
| کارتابل خالی است | داده‌ی نمونه پر نشده | سلول ۵ (بازنشانی) → Refresh |
| صفحه سفید یا فایل خراب | فایل استاتیک `dist` | سلول ۴ را اجرا کنید تا ببینید کدام فایل خطا می‌دهد |
| لینک باز نمی‌شود | تونل مرده | سلول ۳ دوباره؛ یا پنجره‌ی سلول ۲ (نمایش داخل Colab) |
| سرور خوابید | Node کرش کرد | سلول ۶ و سپس سلول ۱ |
| اینترنت قطع شد | فقط تونل می‌میرد | برنامه داخل Colab زنده است → سلول ۲ |
| Colab disconnect شد | runtime ریست شده | `Runtime → Reconnect` سپس سلول ۱ (داده از صفر seed می‌شود) |

**قاعده‌ی طلایی:** اگر چیزی خراب شد، جمله‌ی «بگذارید یک‌بار سرویس را ریست کنم» را بگویید و سلول ۱ را اجرا کنید؛
۳۰ ثانیه است و حرفه‌ای به نظر می‌رسد.

---

## ۷) آماده‌سازی نسخه‌ی تازه (اگر کد را تغییر دادید)

خروجی `dist/` و فایل `Tejaratyarr.zip` در مخزن **از قبل build شده** هستند تا Colab نیازی به
`npm install` نداشته باشد. بعد از هر تغییر کد، این دو را تازه کنید:

```bash
npm ci
npx tsc --noEmit     # بررسی نوع
npm run build        # dist/ را بازسازی می‌کند
zip -rq Tejaratyarr.zip . -x '.git/*' -x 'node_modules/*' -x 'Tejaratyarr.zip' -x 'data/*'
```
